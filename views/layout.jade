doctype html
html
	head
		title babo
		script(src="http://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js")
		link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css')
		link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap-theme.min.css')
		link(rel="stylesheet" type="text/css" href="http://fonts.googleapis.com/earlyaccess/nanumgothic.css")
		link(rel='stylesheet', href='/stylesheets/style.css')
		script(src='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js')
		script(src='https://cdn.socket.io/socket.io-1.3.2.js')
		script.
			$(document).ready(function(){
				var socket = io.connect('http://localhost:4000');
				socket.on('stream', function(tweet){
				$('#tweetd').prepend('<p class="tweet">'+tweet+'</p><br>');
				});
				socket.on('hashTag', function(tweet){
					$('#hashTag').prepend('<p class="tweet">'+tweet+'</p><br>');
				});
				socket.on('favorites', function(tweet){
					$('#favorites').prepend('<p class="tweet">'+tweet+'</p><br>');
				});

				$('.row').on("click", '.tweet', function() {
					alert("test");
				});
			});

	body
		block content